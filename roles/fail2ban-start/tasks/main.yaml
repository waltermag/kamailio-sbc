- name: Start Fail2Ban service
  ansible.builtin.systemd:
    state: started
    name: asterisk
  tags:
    - fail2ban
    - system
- name: Enable Fail2Ban service
  ansible.builtin.systemd:
    enabled: true
    name: asterisk
  tags:
    - fail2ban
    - system
