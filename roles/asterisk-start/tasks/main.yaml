- name: Start Asterisk service
  ansible.builtin.systemd:
    state: started
    name: asterisk
  tags:
    - asterisk
    - system
- name: Enable Asterisk service
  ansible.builtin.systemd:
    enabled: true
    name: asterisk
  tags:
    - asterisk
    - system
